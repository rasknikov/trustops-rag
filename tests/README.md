# Tests

Este diretório separa os testes por finalidade:

- `unit/`: lógica isolada
- `integration/`: integração entre módulos
- `contract/`: contratos de API
- `security/`: controles de segurança
- `evaluation/`: benchmarks e gates de qualidade
